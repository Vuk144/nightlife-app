# Supabase — client-safe values only (exposed in the app bundle).
# Never put the service-role key in here or in any EXPO_PUBLIC_ variable.
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
